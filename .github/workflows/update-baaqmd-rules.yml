name: Update BAAQMD Rules

on:
  schedule:
    - cron: "15 7 * * 1"
      timezone: "America/Los_Angeles"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-baaqmd-rules
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      - name: Restore the previous rules collection
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view baaqmd-current >/dev/null 2>&1; then
            gh release download baaqmd-current \
              --pattern "BAAQMD_Current_Rules.zip" \
              --output latest_download.zip
            unzip -q -o latest_download.zip
          fi

      - name: Update current rules
        run: python baaqmd_rules_updater.py --config config.github.json

      - name: Package updated collection
        run: zip -q -r BAAQMD_Current_Rules.zip BAAQMD_Current_Rules

      - name: Create or update the current release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if ! gh release view baaqmd-current >/dev/null 2>&1; then
            gh release create baaqmd-current \
              --title "Current BAAQMD Rules" \
              --notes "Automatically maintained current BAAQMD rule PDFs, index, change log, and combined bookmarked PDF." \
              --target "$GITHUB_SHA"
          fi
          gh release upload baaqmd-current \
            BAAQMD_Current_Rules.zip \
            --clobber

      - name: Record successful run
        run: |
          mkdir -p status
          printf '%s\n' "$(date -u +'%Y-%m-%dT%H:%M:%SZ')" > status/last_successful_run_utc.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add status/last_successful_run_utc.txt
          if ! git diff --cached --quiet; then
            git commit -m "Record successful BAAQMD update"
            git push
          fi
